# Diabetes-preiction-model
This project is a Machine Learning-based web application that predicts the likelihood of diabetes in a patient using the Support Vector Machine (SVM) algorithm. It is built using Python and leverages medical data to provide quick and reliable predictions.
